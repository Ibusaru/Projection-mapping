# Suggested Commands

- From `C:/Users/scrat/Documents/創造工学/unity/OceanProjection`: `dotnet build Assembly-CSharp.csproj --no-restore` for quick C# compile verification.
- Use PowerShell on Windows. Prefer `rg -n "pattern" Assets` for code searches.
- Avoid Unity batchmode on the project while the editor is already open; it can fail because the project is locked.